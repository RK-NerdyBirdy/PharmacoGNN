# --- Phase 1: API core & database ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic[email]>=2.7
pydantic-settings>=2.3
sqlalchemy>=2.0.30
asyncpg>=0.29
alembic>=1.13
pyjwt>=2.8
passlib[bcrypt]>=1.7
# passlib 1.7.4's bcrypt backend probes bcrypt.__about__.__version__ for version
# detection; bcrypt>=4.1 removed that submodule, which makes passlib misdetect
# the backend and crash on hash() with "password cannot be longer than 72 bytes"
# even for short passwords. Pin below 4.1 until passlib ships a fix.
bcrypt<4.1
cryptography>=42.0
python-dotenv>=1.0
slowapi>=0.1.9
aiosmtplib>=3.0

# --- Phase 2: GNN inference engine ---
torch>=2.3
torch-geometric>=2.5

# --- Phase 3: LLM explainability (OpenRouter) ---
httpx>=0.27

# --- Testing ---
# Tests use FastAPI's synchronous TestClient throughout (it bridges to async
# endpoints internally), so no pytest-asyncio/async test functions needed.
pytest>=8.0
